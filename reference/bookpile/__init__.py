"""Bookpile reference implementation.

Evidence that the specification is buildable. Not the product — the spec and
the conformance vectors are. A generated implementation is equally valid if it
passes the same vectors.
"""
__all__ = ["models", "ports", "service", "intents", "projection"]
